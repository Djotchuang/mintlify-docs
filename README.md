# Fapshi Docs V2

### Development

Install the [Mintlify CLI](https://www.npmjs.com/package/mintlify) and all packages to preview the documentation changes locally. To install, use the following command

```
npm i or npm i --legacy-peer-deps
```

Run the following command at the root of the project (where docs.json is)

```
npx mint dev
```
