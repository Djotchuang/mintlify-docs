---
title: "Get Service Balance"
description: "Returns the current balance of the service account."
keywords: ["service balance", "account balance", "fapshi balance", "payment service", "API balance"]
openapi: 'GET /balance'
---

# Get Service Balance

**Endpoint:** `GET /balance`

Returns the current balance of the service account.

> **Note:** Balance in sandbox environment is randomly generated on each request.
